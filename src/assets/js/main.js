if (typeof window !== "undefined") {
  const bugscribe = new Bugscribe({
    button: {
      position: {
        vertical: "bottom",
        horizontal: "right",
      },
      bgColor: "#ff5733",
    },
  });
}
