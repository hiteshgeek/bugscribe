now what i want should be generated as a separate component and use it here, keep is all js in one folder and all scss in one folder like video-player, the feature i want is image editor, we will add its feature gradually, for the first of its feature i want, on showing the accept/cancel button also add another button for annotate, this itself says the accept is selected, and we have to show new toolbar options

which will have following action buttons, for icons use icons.js

1. Add text (use icon note)
2. Add shape dropup( having options, rectangle, oval)
3. Add pointers
4. Add blur
5. crop image
6. undo (only if any operations performed)
7. done (which adds either modified or unmodified (if not operations performed) image to preview)

do not immediately implement these features, just show then in toolbar and provide tooltips
